// What is array?
// --> An array is the simplest memory data structure. It stores multiple values that are all of the same datatype sequentially. but in js we can store different data types also
// so array is order collection of arrays

let fruits = ["apple", "banana", "jackfruit", "orange"];
console.log("🚀 ~ fruits:", fruits);

console.log(fruits[0]);
console.log(fruits[1]);
console.log(fruits[2]);
console.log(fruits[3]);
